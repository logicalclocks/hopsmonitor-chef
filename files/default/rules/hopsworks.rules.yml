groups:
- name: Hopsworks 
  rules:
  - alert: HopsworksDown 
    expr: absent(vendor:http_service_server_request_count2xx_count) > 0
    for: 1m
    labels:
      type: system-alert
    annotations:
      summary: "Hopsworks is down"
      description: "Hopsworks is not reporting metrics" 
